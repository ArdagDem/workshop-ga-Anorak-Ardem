name: Tests

on:
  push:
    branches:
      - master
      - dev


jobs:
  hello-world:
    runs-on: ubuntu-20.04
    steps:
    - id: Say_hello_world
      run: echo Welcome to the Github Actions workshop !